using UnityEngine;
using System.Collections;

public class BaseCell : MonoBehaviour {

    /* fields:
            background - image;
            list of observers (?);
            type - enum (?);
            
    */
    
     
}
